export enum PROVIDERS {
  CASSANDRA = "CASSANDRA",
}

export enum TOPICS {
  IN_TOPIC = "InTopic",
  OUT_TOPIC = "OutTopic",
}

export enum KAFKA_KEYS {
  GET_ALL = "get.all",
  GET_ONE = "get.one",
  CREATE = "create",
  UPDATE = "update",
  DELETE = "delete",
}
