spring:
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    url: 'jdbc:postgresql://localhost:5434/test-db'
    username: 'postgres'
    password: 'postgres'
    driver-class-name: 'org.postgresql.Driver'
  liquibase:
    change-log: 'liquibase/test/changelog-test.xml'
    liquibase-schema: 'liquibase_schema'
    default-schema: 'distcomp'
